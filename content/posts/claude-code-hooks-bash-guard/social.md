プロンプトに「禁止」と書くだけでは、エージェントは止まりません。指示は読み飛ばされる前提で設計します。

なので「mainへの直接push」「自分のPRの自己マージ」みたいな戻れない操作は、Claude Codeのhooksでコマンドを実行前に検査して、コードの側でブロックしています。判定できない入力はブロックに倒す。すり抜け前提でブランチ保護も残す。

実運用中のBashガードの実装記録をブログにまとめました。
https://tentspace.net/blog/claude-code-hooks-bash-guard
